# Stella MCP 0.13.0 Release Gates

Status: unpublished internal candidate; no `v0.13.0` tag, GitHub release, or
PyPI distribution was created. These measurements are retained as historical
candidate provenance and are included in the cumulative `0.14.0` record.

Local release-candidate verification completed on 2026-07-19 from the final
`codex/v0.13-trust-loop` working tree. The record left GitHub Actions as a
separate gate; no public `0.13.0` release followed.

## Automated Gates

| Gate | Result |
|---|---|
| Complete test suite | 494 passed in 181.38 seconds |
| Isolated test suite with `mcp==1.19.0` and simulation extra | 494 passed in 178.22 seconds |
| Deterministic native MCP evaluation | 5/5 scenarios passed; 19 tool calls; 0 failures or skips |
| Ruff | Passed |
| Lockfile consistency | Passed; 104 packages resolved |
| Numeric evidence regeneration check | Passed |
| Capability evidence regeneration check | Passed |
| Candidate metadata | Local validation passed for the intended `v0.13.0` candidate identity dated 2026-07-18; no tag or publication followed |
| Git diff hygiene | Passed |

## Distribution Gates

- Built local candidate artifacts `stella_mcp-0.13.0-py3-none-any.whl` and
  `stella_mcp-0.13.0.tar.gz` from the audited tree; neither was published.
- Twine metadata checks passed for both distributions.
- Wheel inspection found the expected six bundled template files.
- Source-archive inspection found all 35 runtime modules and six bundled
  template files, with tests and retained evaluation artifacts excluded.
- A clean Python 3.11.9 environment installed the wheel, imported the MCP
  server, reported package version 0.13.0, and strict-parsed all five built-in
  models from packaged resources.

## Retained Manual Evidence

- Stella Professional 4.1.1 opened, ran, exported, saved, and visually reviewed
  all six package-generated desktop cases. Saved models strict-reimported with
  no identifier renames or computational changes. See
  [`0.13.0-desktop-acceptance.md`](0.13.0-desktop-acceptance.md).
- Agent protocol v2 completed three fresh runs for each of three scenarios.
  All nine runs passed every required dimension across 51 MCP calls with no
  failed, recovered, or errored tool outcomes. See
  [`0.13.0-agent-evaluation.md`](../../results/evaluation/0.13.0-agent-evaluation.md).

## Reviewed Boundaries

- Arrays, module instances, and additional top-level models are rejected by
  strict import/export, structurally preserved with warnings in permissive
  round-trips, and rejected before every PySD-backed operation.
- The bundled SIR model uses `transmission_rate` and `recovery_rate`; retained
  Stella saves report no identifier renaming.
- Numeric evidence applies no threshold or interpolation. Every non-zero
  discrepancy remains recorded. The material Lotka-Volterra `predation`
  difference is documented as a Stella/PySD non-negative-flow semantic
  difference, not accepted as parity.
- The scalar graphical-function Stella save contains two serialization
  rewrites but no effective computational change; both rewrites are retained in
  the desktop evidence.
