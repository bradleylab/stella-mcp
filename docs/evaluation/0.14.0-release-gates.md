# Stella MCP 0.14.0 Release Verification

Status: the sanitized release candidate passed local product, evaluation, and
distribution checks. No tag or package publication is represented by this
record.

This document records reproducible product and package checks. Repository
administration, local orchestration state, credentials, and account-level
security configuration are intentionally excluded.

## Protocol and State Checks

| Check | Result |
|---|---|
| SDK range | Passed: `mcp>=2.0.0,<3`; lock resolves `mcp==2.0.0` |
| Modern stdio | Passed: explicit workspace creation, stateful calls, resources, revocation, and classified errors |
| Legacy stdio | Passed: initialization, process-local compatibility state, resources, and classified errors |
| Workspace lifecycle | Passed: isolation, expiry, revocation, bounded tombstones, non-mutating resource reads, and serialized mutation |
| Output contracts | Passed: all 44 tools advertise JSON Schema 2020-12 success contracts |
| Error boundary | Passed: classified errors remain actionable and unexpected exceptions do not expose internal details |
| Code Mode exclusion | Passed: the catalog contains 42 retained tools plus 2 workspace lifecycle tools and no executor |

## Automated Checks

| Check | Result |
|---|---|
| Complete pytest suite | Passed: 522 tests in 234.35 seconds |
| Repository hygiene and release-workflow contracts | Passed: 15 focused tests, including ignored-and-untracked local workflow state |
| Ruff | Passed: full repository |
| Lock consistency | Passed: 106 packages; `mcp==2.0.0`; `cryptography==50.0.0` |
| Deterministic evaluation | Passed: 5/5 scenarios, 19 tool calls, 44 tools, no failures or skips |
| Numeric fidelity and capability matrix | Passed both generated-report checks |
| Release metadata | Passed for `v0.14.0` with release date `2026-08-09` |
| Diff hygiene | Passed with no whitespace errors |

The deterministic tool-catalog SHA-256 is
`c6da45c8cf8f5fbc4cf0f43ca504bb7a568a67b12b248d20afd5843f118709fa`.

## Distribution Checks

- Wheel SHA-256:
  `ee2f532b46649ad8598936466232331948303877c48f1cab0090b1a69d4736b0`.
- Source-distribution SHA-256:
  `9c5cd49897de6bffd9f7d4ca03077c5e8efc7f5e5b4058d545e5de9e7d429d72`.
- Twine metadata validation passed for both artifacts.
- The wheel contains all 6 bundled template files.
- The source distribution contains 35 runtime modules and all 6 bundled
  template files.
- A clean environment installed 29 packages from the wheel, including
  `mcp==2.0.0` and `cryptography==50.0.0`.
- Isolated modern and legacy stdio smoke tests passed with 44 tools and no Code
  Mode tool. Import provenance resolved inside the clean environment rather
  than the source checkout.
- The Stella Professional 4.1.1 desktop smoke remains applicable because the
  tested SIR model blob is byte-identical to the sanitized candidate. The model
  opened and ran to model time `100.00`, and the source file remained
  unchanged.

## Release Boundary

The same source revision must pass hosted CI before it is merged. Tag creation,
GitHub release publication, PyPI publication, and post-publication client checks
remain separate release actions. A public tag or uploaded distribution must not
be moved or replaced to repair a release.
